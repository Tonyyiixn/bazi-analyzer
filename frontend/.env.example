# Copy to .env.local and fill in. Not needed for local development - the app
# falls back to http://127.0.0.1:8000 when this is unset.
#
# On Vercel, set this in Project Settings > Environment Variables to the
# deployed backend's base URL (no trailing slash, no /api/v1 suffix).
VITE_API_URL=
